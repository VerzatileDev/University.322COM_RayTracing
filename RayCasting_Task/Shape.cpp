#include "Shape.h"

Shape::Shape()
{
}

bool Shape::IntersectionOfSphere(glm::vec3 center, float radius, glm::vec3 origin, glm::vec3 direction, float& t)
{
    return true; // Doesn't matter because action happens in classes
}